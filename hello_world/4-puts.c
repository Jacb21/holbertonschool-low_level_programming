#include <stdio.h>
/**
 * main - this prints only the specific text
 * Description This was the function we used to print the text that the exercise gave us
 * Return: 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
