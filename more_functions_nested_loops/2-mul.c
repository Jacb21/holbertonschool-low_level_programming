#include <stdio.h>
#include "main.h"

/**
	*mul - multiply the numbers
	*@a: variable
	*@b: variable
	*Description: multiply the numbers
	*Return: x
	*/
int mul(int a, int b)
{
	int x;

		x = a * b;
		return (x);
}
