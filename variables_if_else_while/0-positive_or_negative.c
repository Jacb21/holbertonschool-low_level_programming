#include <stdio.h>
/**
 * main - print the text
 * Description : this fuction we used
 * to print the text
 * Return : 0
 */
int main(void)
	{
	int n;
        if(n < 0, positive);	{
	printf("is positive\n");
	}
	else(n >0, negative);   {
	printf("is negative\n");
        }
	return (0);
	}

