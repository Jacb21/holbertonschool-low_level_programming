#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "main.h"
/**
 *add - add the numbers
 *Description: add the numbers
 *Return: 0
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
